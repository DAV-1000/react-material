{
  "name": "api",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run clean && tsc --outDir ./dist",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "build:azure": "npm run clean &&  tsc && npm run prepare-functions && npm run copy-meta",
    "prepare-functions": "node scripts/prepare-functions.js",
    "copy-meta": "copyfiles host.json package.json dist/"
  },
  "dependencies": {
    "@azure/cosmos": "^4.5.1",
    "@azure/functions": "^4.0.0",
    "copyfiles": "^2.4.1",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "@types/uuid": "^10.0.0",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
