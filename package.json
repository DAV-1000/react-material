{
  "name": "react-material-root",
  "private": true,
  "scripts": {
    "prestart": "cross-env NODE_EXTRA_CA_CERTS=./certs/cosmos-emulator.pem.cer npm run build:all",
    "start": "swa start",
    "start:all": "concurrently \"npm run start --prefix api\" \"npm run dev --prefix frontend\"",
    "build:all": "concurrently \"npm run build --prefix api\" \"npm run build --prefix frontend\"",
    "build:azure": "concurrently \"npm run build:azure --prefix api\" \"npm run build:azure --prefix frontend\"",
    "test:all": "npm run test --prefix frontend",
    "cosmos:cert": "node ./scripts/download-cosmos-cert.mjs"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.8",
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0"
  },
  "dependencies": {
    "zod": "^4.1.5"
  }
}
