{
  "name": "react-material-root",
  "private": true,
  "type":"module",
  "version": "1.0.0",
  "scripts": {
    "prestart": "npm run build:all",
    "start": "swa start",
    "build:all": "npm run build --prefix common && concurrently \"npm run build --prefix api\" \"npm run build --prefix frontend\""

  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.6",
    "concurrently": "^8.2.2"
  },
   "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "common",
    "frontend",
    "api"
  ]
}
