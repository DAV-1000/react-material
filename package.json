{
  "name": "react-material-root",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build:all",
    "start": "swa start",
    "build:all": "npm run build --prefix common && concurrently \"npm run build:azure --prefix api\" \"npm run build --prefix frontend\"",
    "build:azure": "npm run build --prefix common && concurrently \"npm run build:azure --prefix api\" \"npm run build --prefix frontend\""
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.6",
    "concurrently": "^8.2.2",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "common",
    "frontend",
    "api"
  ],
  "dependencies": {
    "@azure/cosmos": "^4.5.1"
  }
}
